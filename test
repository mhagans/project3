

int test;
float test2;
int check;

int gcd (int v, int u)
{
	int check2;
     //void check3;
	if ( v <= 0)
		return u;
	else
		return gcd( v , u - u / v* v );
	/*note u-(u/v*v) == u mod v */

	if ( x >= 1)
	{
		x = 5;

		return y;
	}
	if (x==2)
		return 3;
}