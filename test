

int check;
int test;



int test;
int arraycheck[2];
int arraycheck[3];

int gcd (int v, int u)
{
	int check2;

     //void check3;
	if ( v <= 0)
		return u;
	else
		return gcd( v , u - u / v* v );
	/*note u-(u/v*v) == u mod v */

	if ( x >= 1)
	{
		x = 5;

		return y;
	}
	if (x==2)
		return 3;
}
void main(void)
{
	int x; int y;
	//void check4;
	x = input();
	y = input();
	output(gcd(x,y));
}